{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "args": ["unused"],
            "cwd": "${workspaceFolder}/scripts",
            "console": "integratedTerminal"
        }
    ]
}
