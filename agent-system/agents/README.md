# Agents have moved

Agent definitions are now registered at `.github/agents/` for VS Code discovery.

This directory (`agent-system/agents/`) is no longer used for active agent definitions.
See `agent-system/AGENTS.md` for the current structure.
